# Lets `docker compose up` run from the repo root without `-f`.
# docker compose auto-loads this file and reads COMPOSE_FILE to locate the
# stack. The path is relative to this directory; the compose file's own
# relative mounts (./oem) resolve against its own folder, so they still work.
COMPOSE_FILE=testing/windows/docker-compose.yml
